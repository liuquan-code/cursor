# 组件不渲染问题排查指南

## 🔍 问题确认

✅ 数据已加载（hasDesignableJson: true）
✅ Schema 存在（hasSchema: true）
✅ Form 已创建（hasForm: true）
✅ 组件已导入（SchemaField, RuntimeProvider 都存在）
❌ **但小程序就是不显示**

## 🎯 最可能的原因

### 原因 1: SchemaField 内部使用的组件未注册（80% 可能性）

**问题**：`snify` 的 `SchemaField` 内部可能使用了某些组件，但这些组件没有在小程序环境下注册。

**检查方法**：
```tsx
// 在控制台查看 schema 中使用的所有组件
useEffect(() => {
  if (designableJson?.schema) {
    const findComponents = (obj: any, components: Set<string>) => {
      if (typeof obj !== 'object' || obj === null) return
      if (obj['x-component']) {
        components.add(obj['x-component'])
      }
      Object.values(obj).forEach(value => {
        if (Array.isArray(value)) {
          value.forEach(item => findComponents(item, components))
        } else if (typeof value === 'object') {
          findComponents(value, components)
        }
      })
    }
    const components = new Set<string>()
    findComponents(designableJson.schema, components)
    console.log('Schema 中使用的组件:', Array.from(components))
  }
}, [designableJson])
```

**解决方案**：使用 `index.final-solution.tsx`，它会：
1. 手动创建 SchemaField
2. 注册所有可能的组件
3. 检查并警告未注册的组件

### 原因 2: snify 的 SchemaField 在小程序环境下有 bug（15% 可能性）

**问题**：`snify/es/components` 的 `SchemaField` 可能在小程序环境下使用了不兼容的 API。

**解决方案**：
- 不使用 `snify` 的 `SchemaField`
- 使用 `@formily/react` 的 `createSchemaField` 手动创建
- 参考 `index.final-solution.tsx`

### 原因 3: 样式问题导致渲染了但看不见（3% 可能性）

**检查方法**：
1. 在小程序开发者工具中查看元素
2. 检查是否有 `display: none`、`height: 0`、`opacity: 0`
3. 检查 `z-index` 是否被遮挡

**解决方案**：
```tsx
<View style={{ 
  minHeight: '100vh',
  width: '100%',
  backgroundColor: '#fff',  // 添加背景色
  position: 'relative',
  zIndex: 1,
}}>
  {/* 组件 */}
</View>
```

### 原因 4: 错误被吞掉了（2% 可能性）

**检查方法**：
```tsx
// 添加错误边界
try {
  return <SchemaField schema={designableJson.schema} />
} catch (error) {
  console.error('渲染错误:', error)
  return <View><Text>错误: {error.message}</Text></View>
}
```

## 🚀 立即尝试的解决方案

### 方案 1: 使用手动创建的 SchemaField（最推荐）⭐⭐⭐⭐⭐

**使用 `index.final-solution.tsx`**

核心思路：
1. 不使用 `snify` 的 `SchemaField`
2. 使用 `@formily/react` 的 `createSchemaField` 手动创建
3. 显式注册所有组件

```tsx
import { createSchemaField } from '@formily/react'
import { View, Text } from '@tarojs/components'
import { Input, Button } from '@taroify/core'

const SchemaField = createSchemaField({
  components: {
    View,
    Text,
    Input,
    Button,
    // 添加所有你使用的组件
  },
})
```

### 方案 2: 使用深度调试版本定位问题 ⭐⭐⭐⭐

**使用 `index.deep-debug.tsx`**

这个版本会：
1. 尝试多种渲染方式
2. 直接渲染 schema 字段（不使用 SchemaField）
3. 使用 FormProvider + Field 手动渲染
4. 使用 CustomSchemaField
5. 使用 snify 的 SchemaField

通过对比哪种方式能渲染，定位问题。

### 方案 3: 检查并修复组件注册 ⭐⭐⭐

**步骤**：
1. 运行 `index.deep-debug.tsx`
2. 查看控制台输出的 "Schema 中使用的组件"
3. 确保所有这些组件都在 `createSchemaField` 的 `components` 中注册

```tsx
// 根据控制台输出，添加缺失的组件
const SchemaField = createSchemaField({
  components: {
    // ... 已有的组件
    // 添加控制台提示的缺失组件
    MissingComponent1,
    MissingComponent2,
  },
})
```

### 方案 4: 绕过 SchemaField，使用 RecursionField ⭐⭐

```tsx
import { FormProvider, RecursionField } from '@formily/react'

return (
  <FormProvider form={form}>
    <RecursionField schema={designableJson.schema} />
  </FormProvider>
)
```

## 📋 排查步骤

### 步骤 1: 运行深度调试版本

1. 使用 `index.deep-debug.tsx` 替换你的代码
2. 查看小程序中哪个测试能显示
3. 如果"测试 1: 直接渲染 Schema 字段"能显示，说明问题在 SchemaField
4. 如果"测试 2: 使用 FormProvider + Field"能显示，说明问题在 SchemaField 的创建
5. 如果"测试 3: 使用 CustomSchemaField"能显示，说明问题在组件注册
6. 如果"测试 4: 使用 snify 的 SchemaField"不能显示，说明 snify 的 SchemaField 有问题

### 步骤 2: 检查组件注册

```tsx
// 在控制台查看
console.log('Schema 中使用的组件:', components)

// 检查是否有未注册的组件
const registeredComponents = ['View', 'Text', 'Input', ...]
const missing = components.filter(c => !registeredComponents.includes(c))
console.warn('未注册的组件:', missing)
```

### 步骤 3: 检查 schema 结构

```tsx
// 检查 schema 是否符合 Formily 的格式要求
console.log('Schema 结构:', JSON.stringify(designableJson.schema, null, 2))

// 检查是否有 properties
if (designableJson.schema.properties) {
  console.log('Properties:', Object.keys(designableJson.schema.properties))
}
```

### 步骤 4: 检查错误

```tsx
// 添加错误捕获
try {
  return <SchemaField schema={designableJson.schema} />
} catch (error) {
  console.error('渲染错误:', error)
  return <View><Text>错误: {error.message}</Text></View>
}
```

## ✅ 检查清单

- [ ] 运行 `index.deep-debug.tsx` 查看哪个测试能显示
- [ ] 检查控制台输出的 "Schema 中使用的组件"
- [ ] 确保所有组件都已注册
- [ ] 检查是否有未注册的组件警告
- [ ] 检查 schema 结构是否正确
- [ ] 检查控制台是否有错误
- [ ] 尝试使用 `index.final-solution.tsx`

## 🎯 最推荐的解决方案

**立即使用 `index.final-solution.tsx`**，它：
1. ✅ 手动创建 SchemaField，确保组件注册正确
2. ✅ 自动检测并警告未注册的组件
3. ✅ 提供了多种渲染方案（注释掉的代码）
4. ✅ 添加了详细的调试日志

如果这个方案还不行，使用 `index.deep-debug.tsx` 定位具体问题。

## 💡 如果还是不行

1. **检查 snify 库的版本**
   - 查看是否有小程序相关的已知问题
   - 考虑升级或降级版本

2. **联系 snify 库的维护者**
   - 询问小程序兼容性问题
   - 提供复现步骤

3. **考虑不使用 snify 的封装**
   - 直接使用 `@formily/react` 和 `@formily/core`
   - 参考 Formily 官方文档

4. **检查 Taro 配置**
   - 确保编译配置正确
   - 检查是否有特殊的编译选项需要设置
