# 小程序不显示问题诊断指南

## 🔍 问题现象

所有状态都是 `true`，但小程序还是不显示：
```
hasDesignableJson: true
hasEventConfig: true
hasEventJson: true
hasForm: true
hasSchema: true
isDataReady: true
```

## 🎯 可能的原因（按优先级）

### 1. **snify 的 SchemaField 未正确注册小程序组件** ⭐⭐⭐⭐⭐

**问题**：`snify/es/components` 中的 `SchemaField` 可能没有注册 Taro/Taroify 组件

**解决方案 A：使用调试版本检查**

使用 `index.debug.tsx`，它会：
- 在每个组件渲染前后添加调试 View
- 帮助你定位到具体哪个组件没有渲染

**解决方案 B：手动创建 SchemaField**

使用 `index.solution.tsx`，它会：
- 使用 `@formily/react` 的 `createSchemaField` 手动创建
- 显式注册所有 Taro/Taroify 组件

```tsx
import { createSchemaField } from '@formily/react'
import { View, Text } from '@tarojs/components'
import { Input, Button } from '@taroify/core'

const CustomSchemaField = createSchemaField({
  components: {
    View,
    Text,
    Input,
    Button,
    // 注册所有 schema 中使用的组件
  },
})
```

### 2. **样式问题导致组件被隐藏** ⭐⭐⭐⭐

**检查方法**：
1. 在小程序开发者工具中查看元素
2. 检查是否有 `display: none` 或 `visibility: hidden`
3. 检查 `height: 0` 或 `opacity: 0`

**解决方案**：
```tsx
<View style={{ 
  margin: 0, 
  padding: 0, 
  minHeight: '100vh',  // 确保有最小高度
  backgroundColor: '#f5f5f5'  // 添加背景色便于查看
}}>
  <FormPage form={form} {...designableJson.form}>
    {/* ... */}
  </FormPage>
</View>
```

### 3. **FormPage 组件问题** ⭐⭐⭐⭐

**检查方法**：
- 尝试不渲染 `FormPage`，直接渲染 `SchemaField`
- 检查 `FormPage` 的 props 是否正确

**解决方案**：
```tsx
// 测试：不渲染 FormPage
return (
  <View>
    <CustomSchemaField schema={designableJson.schema} />
  </View>
)
```

### 4. **RuntimeProvider 组件问题** ⭐⭐⭐

**检查方法**：
- 尝试不渲染 `RuntimeProvider`，直接渲染 `SchemaField`
- 检查 `store` 是否正确

**解决方案**：
```tsx
// 测试：不渲染 RuntimeProvider
return (
  <View>
    <FormPage form={form} {...designableJson.form}>
      <CustomSchemaField schema={designableJson.schema} />
    </FormPage>
  </View>
)
```

### 5. **Schema 数据结构问题** ⭐⭐⭐

**检查方法**：
```tsx
console.log('schema 内容:', JSON.stringify(designableJson.schema, null, 2))
console.log('schema 类型:', typeof designableJson.schema)
console.log('schema 是否为数组:', Array.isArray(designableJson.schema))
```

**可能的问题**：
- Schema 格式不正确
- Schema 为空对象 `{}`
- Schema 为数组但为空 `[]`

## 🔧 诊断步骤

### 步骤 1：使用调试版本

1. 使用 `index.debug.tsx` 替换你的代码
2. 查看小程序中是否显示调试信息
3. 确定渲染到哪一步停止了

### 步骤 2：逐步简化组件

```tsx
// 第 1 步：只渲染 View
return <View><Text>测试</Text></View>

// 第 2 步：添加 FormPage
return (
  <View>
    <FormPage form={form}>
      <Text>FormPage 内部</Text>
    </FormPage>
  </View>
)

// 第 3 步：添加 SchemaField（不使用 RuntimeProvider）
return (
  <View>
    <FormPage form={form}>
      <CustomSchemaField schema={designableJson.schema} />
    </FormPage>
  </View>
)

// 第 4 步：添加 RuntimeProvider
return (
  <View>
    <FormPage form={form}>
      <RuntimeProvider eventConfig={eventJson.present.data}>
        <CustomSchemaField schema={designableJson.schema} />
      </RuntimeProvider>
    </FormPage>
  </View>
)
```

### 步骤 3：检查控制台错误

在小程序开发者工具中：
1. 打开 Console 面板
2. 查看是否有错误信息
3. 特别关注：
   - `Component is not registered`
   - `Cannot read property`
   - `TypeError`

### 步骤 4：检查网络请求

1. 打开 Network 面板
2. 确认数据请求成功
3. 检查返回的数据格式

### 步骤 5：检查样式

1. 打开 Elements 面板（或类似功能）
2. 查看组件是否渲染但被隐藏
3. 检查样式属性

## 🚀 快速修复方案

### 方案 1：使用手动创建的 SchemaField（推荐）

```tsx
import { createSchemaField } from '@formily/react'
import { View, Text } from '@tarojs/components'
// 导入所有你使用的 Taroify 组件

const CustomSchemaField = createSchemaField({
  components: {
    // 注册所有组件
  },
})

// 使用 CustomSchemaField 替代 snify 的 SchemaField
<CustomSchemaField schema={designableJson.schema} />
```

### 方案 2：检查并修复样式

```tsx
<View style={{ 
  minHeight: '100vh',
  width: '100%',
  backgroundColor: '#fff'  // 添加背景色
}}>
  {/* 你的组件 */}
</View>
```

### 方案 3：简化组件结构

```tsx
// 先测试最简单的结构
return (
  <View style={{ padding: '20px' }}>
    <Text>测试文本</Text>
    <CustomSchemaField schema={designableJson.schema} />
  </View>
)
```

## 📋 检查清单

- [ ] 使用 `index.debug.tsx` 查看调试信息
- [ ] 检查控制台是否有错误
- [ ] 检查组件是否渲染但被隐藏（样式问题）
- [ ] 尝试手动创建 `SchemaField` 并注册组件
- [ ] 尝试不渲染 `FormPage` 和 `RuntimeProvider`
- [ ] 检查 `schema` 的数据结构是否正确
- [ ] 检查样式文件是否正确引入
- [ ] 检查是否有其他组件遮挡

## 🎯 最可能的原因

根据你的情况（所有状态都是 true），**最可能的原因是 `snify` 的 `SchemaField` 没有正确注册小程序组件**。

**立即尝试**：
1. 使用 `index.solution.tsx`（手动创建 SchemaField）
2. 或者使用 `index.debug.tsx` 查看具体哪个组件没有渲染

## 💡 调试技巧

### 添加边界标记

```tsx
return (
  <View>
    <View style={{ backgroundColor: 'red', padding: '10px' }}>
      <Text>开始渲染</Text>
    </View>
    
    <FormPage form={form}>
      <View style={{ backgroundColor: 'yellow', padding: '10px' }}>
        <Text>FormPage 内部</Text>
      </View>
      
      <CustomSchemaField schema={designableJson.schema} />
      
      <View style={{ backgroundColor: 'green', padding: '10px' }}>
        <Text>SchemaField 之后</Text>
      </View>
    </FormPage>
    
    <View style={{ backgroundColor: 'blue', padding: '10px' }}>
      <Text>结束渲染</Text>
    </View>
  </View>
)
```

这样你就能看到渲染到哪一步停止了。
